import { Box, Typography } from "@mui/material";
import React from "react";

const AssignComponent = ({ reservation }) => {
  return (
    <Box>
      <Typography>-</Typography>
    </Box>
  );
};

export default AssignComponent;
